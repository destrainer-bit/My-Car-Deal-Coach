{
  "builds": [
    { 
      "src": "vite.config.js", 
      "use": "@vercel/static-build", 
      "config": { "distDir": "dist" } 
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "build": {
    "env": {
      "NPM_CONFIG_IGNORE_OPTIONAL": "false"
    }
  }
}
